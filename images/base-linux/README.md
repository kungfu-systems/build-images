# base-linux

Common Linux baseline for Kungfu build images.

This image intentionally stays small:

- Ubuntu 24.04 pinned by digest;
- certificate and Git basics;
- UTF-8 locale;
- non-root `kungfu` build user.

